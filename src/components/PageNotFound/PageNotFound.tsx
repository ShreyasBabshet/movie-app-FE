import styles from "./PageNotFound.module.scss";
import { PageNotFoundProps } from "./PageNotFound.types.ts";

const PageNotFound = ({}: PageNotFoundProps) => {
  return <div>Page Not Found</div>;
};

export default PageNotFound;
